## Ey-Ai

`Ey-AI` is a lightweight and high-performance chatbot REST API framework focused on simplicity, modularity, and speed.

### Example usage

```rust
let gemini = GeminiClient::new();
let key = gemini.initiate(env_key);
gemini.eyai_wrapper(key, "Hi");
```

## Coming soon!
